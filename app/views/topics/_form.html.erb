<div class="row">
  <div class="large-8 columns large-centered">
    <%= form_for(topic, url: url) do |f| %>
      <%= f.text_field :name %>
      <%= f.text_field :description %>
      <br>
      <%= f.submit "Create" %>
    <% end %>
  </div>
</div>
